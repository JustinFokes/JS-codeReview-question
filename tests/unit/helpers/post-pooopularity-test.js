import { postPooopularity } from 'question/helpers/post-pooopularity';
import { module, test } from 'qunit';

module('Unit | Helper | post pooopularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postPooopularity([42]);
  assert.ok(result);
});
